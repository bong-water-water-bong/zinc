# Effort 28 — Multi-tenant serving + continuous (request-level) batching on CUDA

> **Status:** 🔬 OPEN (spawned 2026-06-14). Goal: ZINC+CUDA serves **many concurrent requests batched into one GPU forward** (continuous/dynamic batching, the vLLM/TGI model), not one-sequence-at-a-time. This is a **multi-cycle BUILD on ONE persistent branch `feat/e28-batching`** (NOT independent perf tweaks, NOT main). Each cycle advances the next increment, validates it, commits to `feat/e28-batching`, and updates `## CURRENT STATE` below.

Forward paths: `src/compute/forward_cuda_gemma.zig` (gemma4 dense+MoE — **the increment-1 target**), `src/compute/forward_cuda.zig` (qwen35/36 hybrid-SSM — increment 4). Serving: `src/server/routes.zig`, `src/server/http.zig`, `src/main.zig`. Scaffolding (exists, UNWIRED in CUDA): `src/scheduler/{scheduler,request,kv_cache}.zig`.

## Where we are today (the honest baseline — confirmed by code audit 2026-06-14)

- **Server is thread-per-request but GPU-SERIALIZED.** `routes.zig` spawns a thread per connection, but every generation funnels through one global `ServerState.generation_mutex` (`routes.zig:351,554,605`; `GenerationGuard` at `:346`). Concurrent clients → correct, isolated outputs, but the GPU runs **one sequence at a time**; the rest queue (head-of-line blocking). So multi-tenant *correctness* exists; multi-tenant *throughput* does not.
- **`--parallel N` (default 4, `main.zig:171,606`) is cosmetic** — parsed + logged "max N concurrent requests" (`main.zig:2198/2426`), only sizes the unused scheduler's slot array. Batches nothing.
- **CUDA forward is single-sequence.** `decodeStep(token, pos)` (`forward_cuda_gemma.zig:638`, `forward_cuda.zig:456`); monolithic per-layer KV `kv_k[L] = [max_ctx·kv_dim]` indexed `pos·kv_dim` (`forward_cuda.zig:258,657`). No batch/sequence dimension.
- **`src/scheduler/`** has a `Scheduler` (slot accounting), `Request` lifecycle, and a real paged `KvPagePool` — but the module header says *"the batched prefill/decode dispatch loop is not wired yet,"* `pendingPrefill()`/`activeDecoding()` are stubs returning `&.{}`, and it's only `_ = @import`'d (dead) in the CUDA path. `KvPagePool` IS used by the generic `forward.zig` (CPU/Metal) but NOT by `forward_cuda*.zig`.

## Why this is the single biggest decode-throughput lever (not just a serving feature)

Decode is **GPU-launch / weight-bandwidth-bound at ~7–12% util** (established across Efforts 23/25): each step re-reads the **entire model weights** to emit ONE token. Batching B sequences reads those **same weights once** and amortizes the **same kernel launches** across B tokens → throughput scales ~linearly in B until compute-bound. So the very thing that caps single-stream decode (it's bandwidth/launch-bound, not compute-bound) is exactly what makes request-batching a **multi-× aggregate-throughput win**. `llama-server` does continuous batching — so this is the *real* "beat llama" comparison for serving, and where ZINC has the most headroom.

## THE KEY INSIGHT — batched decode ≈ the batched PREFILL we already have

`prefillBatched` (`forward_cuda_gemma.zig:729`) **already runs a B-wide forward correctly**: B token-rows through `attentionLayerBatched` (`:936`) + `ffnBlockBatched` (`:1015`), with the qkv/o/ffn **GEMMs already sized for B rows** (`BatchScratch` buffers `[T, …]`). Decode-batching reuses ALL of it. The **only** single-sequence assumptions are in TWO spots:

1. **The attention kernel `gemma_attention_batched`** (`kernels.cu:3150`): `blockIdx.y = t` is the query row, and it hard-codes `seq_len = t + 1u` (causal length = the query's position *within the prompt*) over **one shared KV** base. → Generalize to: query row `b` belongs to sequence `b` at **its own** position `positions[b]`, reading **its own** KV slot. So `seq_len = positions[b] + 1`, and the K/V base gets a per-sequence slot offset. The sliding-window logic (`:3164`) carries over per-sequence unchanged.
2. **KV write + KV layout**: today one sequence writes `kv_k[L]` at `pos·kv_dim`. → slot-based: sequence in slot `s` at pos `p` writes `(s·max_ctx + p)·kv_dim`.

Everything else (norms, RoPE, GEMMs, GeGLU, sampling) is already per-row over B, or is a trivial per-row grid. **So increment 1's genuinely-new GPU work = generalize ONE attention kernel + slot-index the KV write. The rest is buffer sizing + a driver + a harness.** This is what makes a from-scratch serving engine unnecessary.

## CURRENT STATE (read this FIRST; update it LAST every cycle)

**PERF LEVER (this cycle) — B==1 matvec fast path PORTED to qwen `decodeBatch` (`forward_cuda.zig`), ~13× the B=1 path; commit `2cb95904`.** The gemma B==1 fast path (`0fee12cc`) had a 1:1 qwen analog (qwen's 4c `gemmDispatch` over B rows runs at B==1 for every per-token prefill + single-client decode → wastes 63/64 of the 64×64 tile). FIX (additive, mirrors gemma): a `decode_b1` flag on `ForwardCuda` (set in `decodeBatch` ONLY when `B==1` AND `b1MatvecOn()`, default-on opt-out `ZINC_BATCH_B1_MATVEC=0` — the SAME env knob as gemma; `defer`-reset so it never leaks past the call) + a guard at the TOP of `gemmDispatch`: `if (B==1 and self.decode_b1) → dmmvDispatch` (the SAME tuned matvec `decodeStep` uses). KEY DIFF vs gemma: the qwen guard passes `acc_mode` THROUGH (qwen's O-proj / FFN-down / SSM-out GEMMs use `acc_mode=1` for the residual, unlike gemma's all-acc_mode-0 decode GEMMs which let it hard-code 0); `dmmvDispatch` already honors `acc_mode`+`a_offset`(=0 at B==1, contiguous) and handles EVERY decode weight type incl. f32 (`dmmvIdx` f32→4 = `dmmv_f32`; production `decodeStep`'s SSM projections already route f32 through it). The fused per-seq attn/SSM `_seq` kernels (grid.y/z=B) already work at B=1 and are unchanged. Harness-only `decode_b1_force: ?bool` + a `B1_TIMING` A/B in the `dbg_cuda batch` QWEN branch (mirrors gemma) time both paths in ONE model load. Production `decodeStep`/`prefillStep` UNTOUCHED — serial path never sets `decode_b1`. **Validated 5090** (cuda-dbg md5 `07e46933` ≠ gemma-fast-path `bb3982e1`): catalog **5/5 serial token-correct** (12/12 each); qwen35-9b 3 seqs mixed positions (4/3/5) **BATCH_GATE PASS + BATCH_SANITY PASS** (PASS-A-solo runs B=1 `decodeBatch` → exercises the matvec path; token-identical batched==solo==serial); **B1_TIMING in-process A/B (NG=64): matvec OFF 4.52 → ON 59.98 tok/s (~13.3×)**. Both gemma (4d→`0fee12cc`) AND qwen (`2cb95904`) `decodeBatch` now have the B==1 fast path → no per-stream penalty at low B for the whole catalog. STILL OPEN at HIGH B (BOTH archs): the head-to-head's B=8 gap is the batched-GEMM compute-inefficiency at B≥2 (matvec path doesn't apply) — separate lever (graph-capture / better batched GEMM).

**PRIOR PERF LEVER — B==1 matvec fast path in gemma `decodeBatch`, ~21× the B=1 path; commit `0fee12cc`.** Pivoted off inc-4 server-wiring to the higher-EV perf lever the 2026-06-15 head-to-head flagged (zinc serving ~30-42× behind llama-server; the architecture is proven-correct+scales but the per-step kernel is inefficient, and the per-STREAM path is ~4× slower than the tuned single-stream `decodeStep`). ROOT CAUSE found by code-read: every B==1 `decodeBatch` step — **all per-token prefill AND any single-client decode** — ran its per-layer projection/FFN through the 64×64-tiled batched TC GEMM (`gemm_q4k_tc_f16a_lowsmem`), which processes ONE row and wastes 63/64 of its tile (cuBLAS self-gates off at T=1<128). FIX (additive, surgical): a `decode_b1` flag on `ForwardGemma` — set in `decodeBatch` ONLY when `B==1` AND `b1MatvecOn()` (default-on, opt-out `ZINC_BATCH_B1_MATVEC=0`), `defer`-reset so a stale true never leaks into a later `prefillBatched` — plus a guard at the TOP of `gemmDispatchA`: when `T==1 and self.decode_b1`, route to `dmmvDispatch` (the SAME tuned matvec `decodeStep` uses). Drop-in because every decode-path GEMM (attn Q/K/V/O + FFN gate/up/down) overwrites y (acc_mode 0) and is contiguous (a_offset 0) at T==1. The fused per-seq attention kernels (`rms_norm_rope_qkv_seq`/`gemma_attention_batched_seq`) already work at B=1 (grid.y=1) and are unchanged. A harness-only `decode_b1_force: ?bool` override lets `dbg_cuda batch` time both paths in ONE model load (boost-comparable A/B). Production `decodeStep`/`prefillBatched` UNTOUCHED — serial path never sets `decode_b1`. **Validated 5090** (cuda-dbg md5 `bb3982e1` ≠ 4d `53992af4`): catalog **5/5 serial token-correct** (12/12 each); gemma-31b 3 seqs mixed positions **BATCH_GATE PASS + BATCH_SANITY PASS** (PASS-A-solo runs B=1 `decodeBatch` → exercises the matvec path; token-identical batched==solo==serial); **B1_TIMING in-process A/B: matvec OFF 2.19 → ON 46.37 tok/s** (the OFF figure matches the documented "B=1 ≈2.2" exactly → the fast path eliminates the batched-GEMM-at-B=1 penalty). NOTE: ON 46 tok/s even exceeds the older "decodeStep ≈8" figure (different warm/boost context; the async fused-kernel attention path helps) — the clean claim is the boost-comparable one-load ratio, not the cross-context 46-vs-8. This fixes the crossover (batched serving no longer loses to single-stream at low B) AND speeds ALL per-token prefill 4-21×. STILL OPEN at HIGH B: the head-to-head's B=8 gap is the batched-GEMM compute-inefficiency at B≥2 (where the matvec path doesn't apply) — separate lever (graph-capture / better batched GEMM). The qwen `decodeBatch` (`forward_cuda.zig`) has the SAME B==1 penalty → port this fast path there next (it's the analog change in its `gemm`/`gemmDispatch`).

**Increment in progress:** 4 — qwen/MoE serving. **4d DONE + validated (cycle 13) → qwen `decodeBatch` now handles MoE (qwen36-35b-a3b), so it is correctness-complete for BOTH dense qwen35 AND MoE qwen36.** Lifted the `if (d.n_experts>0) return error.Unsupported` guard in `ForwardCuda.decodeBatch`; the per-layer FFN now routes to a new `moeFfnBlockBatchedDecode` when `n_experts>0` (else `ffnBlockBatchedDecode`). KEY: the MoE FFN is STATELESS (no KV, no SSM recurrent state — reads `hidden`, routes to experts, accumulates back into `hidden`), so unlike attn/SSM it needs NO per-slot state and NO per-seq position → each of the B rows is an INDEPENDENT single-token MoE FFN. `moeFfnBlockBatchedDecode` loops the rows, aliases row b's `db.hidden` slice, and runs the production `moeFfnBlock` body VERBATIM on it via `moeFfnRowDecode` (rms_norm → router top-k → routed experts SwiGLU+down → weighted combine → shared expert; batched-q4k/q5k fast path OR per-quant host-id fallback, byte-identical to production), reading/accumulating into the row alias + a synchronous `waitPending` per row so the shared per-row scratch (`ffn_norm_buf`/`gate_buf`/`host_router_ids`/…) is safe to reuse next row → **batched==N-serial token-identical by construction** (mirrors the 4b→4c method for attn/SSM). `dbg_cuda batch` qwen branch drops the MoE skip → runs the same PASS-A-solo/PASS-B-batched proof on qwen36-35b-a3b. Production `moeFfnBlock`/`decodeStep`/`prefillStep` UNTOUCHED. 4c (attn+SSM fused, all GEMMs batched) + 4a + 4b + Increments 1 + 2 + 3 COMPLETE; **the qwen `decodeBatch` decode step is now correctness-complete for the whole catalog.** NEXT = **wire qwen into the server** (`runCudaServe`/`ServeEngine` are currently gemma-dense-only — generalize the engine's per-model decode call + slot-state alloc to dispatch ForwardCuda(qwen)/ForwardGemma by arch, so the HTTP server serves qwen35/36 + MoE, not just gemma-dense) — OR the SEPARATE perf lever (B==1 matvec fallback / graph-capture batched step / the per-row MoE loop's launch collapse). NOTE 4d is the CORRECTNESS step — `moeFfnBlockBatchedDecode` still LOOPS rows per-token (each row a full MoE FFN, weights NOT yet read once across B); fusing the router GEMM `[B,n_experts]` + `build_expert_order` over B·n_used pairs into batched expert matvecs is a later throughput sub-step (the MoE analog of 4c's GEMM/attn/SSM amortization).

**4c-step-1 (this cycle) gave qwen the batched-GEMM amortization the spec calls for — the qwen analog of gemma's 1b structure.** Qwen `forward_cuda.zig` on this branch had NO batched-GEMM path (e26's qwen prefillBatched is on main, not this branch fork-point), so this cycle BUILT it: new `gemm:[4]CudaPipeline`+`gemm_f32` (the generic `gemm_*_tiled_v2` kernels, already in kernels.cu, used by gemma — token-major `[T,M]` out, `acc_mode` for the residual), a `GemmPush`, a `gemmDispatch(w,x,y,M,K,B,acc_mode)`, and a lazily-allocated token-major `[B,dim]` `DecodeBatch` scratch (dedicated `ssm_delta`/`ssm_y` buffers so the SSM intermediates never alias with conflicting strides). `decodeBatch` REWRITTEN layer-major: embed all B rows into `db.hidden` slices (same embed kernel as serial), then per layer the pre-norm + Q(+gate)/K/V/O, ssm qkv/z/alpha/beta/out, and ffn gate/up/down run as ONE `gemm` each over B rows (weights read once); the per-seq inner (deinterleave/q-k-norm/RoPE/KV-write/softmax/gate for attn; conv1d/delta_net/gated_norm for ssm) still LOOPS per row on row b's slice + its slot KV / conv ring / recurrent state (block math copied verbatim → token-identical). FFN is FULLY batched. Per-row argmax tail. The 4b looped helpers `attentionLayerSlot`/`ssmLayerSlot` are now superseded/dead (kept as reference). Production `decodeStep`/`prefillStep`/single-seq buffers UNTOUCHED; the serial catalog path never runs the new `gemm` pipelines. NOTE: GEMM-vs-matvec reduction order makes the batched path ARGMAX-identical, not bit-identical (gate is token-identical, PASSED).

**4b (this cycle) built the qwen batched-decode driver — the qwen analog of inc-1's 1b.** New `ForwardCuda.decodeBatch(tokens[B],positions[B],slots[B],out[B])` (`forward_cuda.zig`, additive) for DENSE qwen35: a per-row loop runs each sequence's forward against its OWN slot state via two slot-variant helpers whose block math is copied VERBATIM from production `attentionLayer`/`ssmLayer` (so batched==N-serial is token-identical by construction). `attentionLayerSlot(L,pos,slot)` — KV aliased to the slot's region (`kv_base = slot·slot_ctx·kv_dim·4`), kv-write + `naive_attention` pos-relative within the slot (`seq_len=pos+1`, sinks + sigmoid-z gate), synchronous `commitAndWait` (keeps the per-row slot aliases valid). `ssmLayerSlot(L,pos,slot)` — conv ring + recurrent state aliased to the slot (`slotConvOffsetBytes`/`slotStateOffsetBytes`), the genuinely-new hybrid-SSM piece; conv ring offset DERIVED `conv_off = pos % (d_conv-1)` (production advances `conv_off[L]` one step/token → equals `pos%(d_conv-1)`; deriving it makes each slot self-consistent without per-slot host state). MoE (`d.n_experts>0`) → `error.Unsupported` (qwen36 is 4d). Also added `resetState()` (zero the production single-seq SSM conv+recurrent state + clear `conv_off`) used by the harness to make each SERIAL reference TRULY single-sequence — without it qwen's unindexed SSM recurrent state leaks from the prior reference sequence; no-op for gemma's position-indexed KV. The `dbg_cuda batch` qwen branch now runs the 4a slot-state smoke, then the SAME PASS-A-solo / PASS-B-batched proof as gemma (per-token B=1 prefill into each slot, then decode all seqs together at MIXED positions) → `BATCH_GATE`(batched==solo) + `BATCH_SANITY`(solo==serial). Production `decodeStep`/`prefillStep`/single-seq buffers UNTOUCHED.

**4a (prior cycle) plumbed the per-sequence slot state — the qwen analog of inc-1's 1a.** Added to `ForwardCuda` (`forward_cuda.zig`, qwen35/36 hybrid-SSM) ADDITIVE per-sequence slot buffers + helpers: `kv_k_slots`/`kv_v_slots` (attn layers, `[n_slots·slot_ctx·kv_dim]`) + `ssm_conv_slots`/`ssm_state_slots` (ssm layers, `[n_slots·conv_state_len]` / `[n_slots·ssm_state_len]`, zero-init) + `n_slots`/`slot_ctx`; `allocSlotState`/`freeSlotState` (uniform stub for the wrong layer type, re-callable), `slotKvOffsetBytes(slot,pos)`/`slotConvOffsetBytes(slot)`/`slotStateOffsetBytes(slot)`, and `slotStateSmoke` (non-overlap round-trip proving distinct slots → distinct device regions for KV, the conv ring, AND the recurrent state). The `dbg_cuda batch` qwen branch (was a bare skip) now allocs slot state + runs the smoke. The genuinely-new hybrid-SSM piece vs gemma's slot KV: the **per-slot SSM conv + recurrent state** (gemma is attention-only). Production single-sequence `kv_k`/`kv_v`/`ssm_conv_state`/`ssm_state` + `decodeStep`/`prefillStep` UNTOUCHED. **The qwen serial reference `BATCH_SEQ` (production prefill+decodeStep) the 4b proof must reproduce is now emitted by the harness and cross-checks ==`gen`.**

**3c proved the HEADLINE: request-batching scales aggregate decode throughput ~linearly.** Drove the running `zinc` CUDA server with B=1/2/4/8 concurrent clients; new `/stats` counters (worker-only writes, atomic reads) let the gate diff two snapshots around each B-phase → pure server-side decode tok/s + mean batch occupancy. On gemma-31b dense (5090, ctx=1024, NG=96, nslots=8, 3 rounds, budget-only EOS): **srv decode tok/s 2.2 / 4.2 / 8.3 / 15.9 = 1.00 / 1.95 / 3.81 / 7.34× vs B=1**, occupancy 1.00 / 1.98 / 3.96 / 7.92 (batch fills to B) → **TP_GATE PASS**. This IS the core Effort-28 thesis demonstrated end-to-end over the real HTTP server: B sequences read the weights ONCE and amortize launches across B tokens.

**3b stood up the FIRST CUDA HTTP server.** `zinc -m gemma.gguf` with NO `--prompt` now runs a real multi-tenant HTTP/SSE server (previously `main.zig:1662` errored "server mode not yet wired on CUDA"). `routes.zig`/`InferenceEngine` are Vulkan/Metal-only, so per the plan's option (a) we brought up a minimal CUDA serving path directly in `main.zig` (`runCudaServe`) backed by a reusable engine module — NOT via routes.zig. The single-sequence prompt path is unchanged.

**Done so far:**
- **Increment 1 COMPLETE** — `decodeBatch(tokens[B], positions[B], slots[B], out[B])` for dense gemma-31b: batched projections/FFN (weights read once over B rows) + two batched per-seq attention kernels (`rms_norm_rope_qkv_seq` + `gemma_attention_batched_seq`, grid.y=b, per-seq `positions[]`/`slots[]`), 4 batched launches + single tail `waitPending`. Token-identical batched==solo==prod-gen (`b251442d` 1a, `51b4af11` 1b, `708e798a` 1c).
- **Increment 2 COMPLETE** — `src/scheduler/scheduler.zig` wired as a real continuous-batching scheduler (additive): `pending` FIFO + `scratch`; `enqueue`/`admitNext`/`hasFreeSlot`/`isIdle`; real `pendingPrefill()`/`activeDecoding()`. `dbg_cuda sched` harness (`schedMode`) drives the ragged running batch (admit→prefill→`.decoding`, ONE `decodeBatch`/tick over all decoders, EOS/budget evict→free→admit waiter, variable per-request lengths). `SCHED_GATE` = every scheduled stream token-identical to its isolated run incl. EOS-truncated length (`1add3049` 2a, `537306f2` 2b).
- **3a DONE** — the server's THREADING MODEL, proven in isolation (no HTTP transport yet). New `dbg_cuda serve <seqs> <ngen> <nslots> [model]` harness (`serveMode` + `serveWorker`/`serveClient`/`serveFinish` + `ServeCtx` in `src/dbg_cuda.zig`): ONE GPU **worker thread** runs the admit→prefill→`decodeBatch`→evict loop (reusing the SAME `Scheduler` API + `decodeBatch`), fed by **N concurrent producer threads** that `enqueue` independently and block for their own stream via a mutex+condvar handoff. Thread-safety: ALL GPU work is confined to the worker thread (the CUDA shim rebinds the context per call — `cuCtxSetCurrent` at every shim entry), so the only cross-thread state is the scheduler `pending` FIFO + the result/done registry, both under ONE mutex; slot prefill/decode/append/release are worker-only and lock-free. Per-sequence isolation (inc 1) means the nondeterministic concurrent admit/interleave order can't change any stream → exactly what the gate asserts. New host test `Scheduler concurrent enqueue under external mutex assigns unique slots`. Production `decodeStep`/`decodeBatch`/`prefillBatched` + the `sched`/`batch` harnesses UNTOUCHED. Commit `d7a188d7`.
- **3b DONE + validated** (this cycle) — the **first CUDA HTTP/SSE server**. New reusable engine `src/server/cuda_serve.zig::ServeEngine`: ONE GPU worker thread runs the admit→per-token-prefill→batched `decodeBatch`→evict loop (reuses `Scheduler` + `decodeBatch` from inc 1+2); a per-request `ReqChannel` registry (keyed by request id, under ONE mutex) delivers each request's tokens **INCREMENTALLY** as produced — the genuinely-new piece vs 3a (which delivered the whole stream only at finish). All GPU/slot work is worker-only + lock-free; handlers `submit` (enqueue + register + broadcast) and `nextChunk` (block until new tokens, copy out UNDER the lock since the worker may realloc `tokens`). `main.zig::runCudaServe` (reached when `zinc -m gemma` has NO `--prompt`): per-connection handler threads tokenize → `submit` → SSE-stream their own tokens; routes `/v1/completions`, `/v1/chat/completions`, `/health`. `ZINC_SERVE_DEBUG_IDS=1` = raw-token-id-in / token-id-out contract for exact gating; `ZINC_SCHED_EOS` overrides the stop token. Additive: production paths + the `serve`/`sched`/`batch` harnesses + the dbg `serveMode` UNTOUCHED. New gate `scripts/validate_cuda_serve.sh`. Commit `6a290c0f`.
- **3c DONE + validated** (this cycle) → **INCREMENT 3 COMPLETE**. The aggregate-throughput gate. Added `ServeEngine` throughput counters (`decode_steps`/`decode_tokens`/`decode_wall_ns` + `prefill_*` + `peak_batch`, worker-thread-only writes via `fetchAdd`, `/stats` handler reads via atomic load — lock-free, never contends the worker); `decode_tokens` SUMS the batch occupancy `ndec` per batched `decodeBatch` step, so over a phase `Δdecode_tokens/Δdecode_wall_s` = aggregate decode tok/s and `Δdecode_tokens/Δdecode_steps` = mean occupancy. New `GET /stats` route (`main.zig` → `statsJson`). New `scripts/throughput_cuda_serve.sh` builds + starts the server once, warms, then runs B=1/2/4/8 concurrent-client phases × ROUNDS, diffs `/stats` per phase. Additive: production `decodeStep`/`prefillBatched` + the `serve`/`sched`/`batch` harnesses UNTOUCHED. Commit `8d29967d`.
**Validated on the 5090** (this cycle, fresh isolated-cache build, `zinc` bin md5 `6c2ab9a41b5fa2d64acd936b88833fbe` ≠ 3b `6e625344`): catalog **5/5 serial token-correct** (12/12 each — production prompt path additive-only). 3c throughput gate (gemma-31b dense, ctx=1024, NG=96, nslots=8, 3 rounds, budget-only EOS): **srv decode tok/s 2.2/4.2/8.3/15.9 (1.00/1.95/3.81/7.34× vs B=1), occupancy 1.00/1.98/3.96/7.92 → TP_GATE_SCALING PASS + TP_GATE_OCCUPANCY PASS + TP_GATE PASS.** Near-linear aggregate scaling to B=8.
**(prior, 3b) Validated on the 5090** (`zinc` bin md5 `6e625344a3056dda5e3b27f0b14dc93f`): catalog **5/5 serial token-correct** (12/12 each — production prompt path unchanged). HTTP serve gate, B=4 mixed-length raw-token-id prompts, `nslots=2` (slot reuse), `NG=10`, budget-only EOS: **SERVE_HTTP_GATE_A PASS** (concurrent N=4 streams == the same prompts run one-at-a-time B=1 through the server) + **SERVE_HTTP_GATE_B PASS** (server B=1 streams == `dbg_cuda serve` SERVE_SEQ reference, itself == isolated decodeStep) — every client's SSE stream token-identical across all 4 seqs. Chain: concurrent HTTP == isolated == llama-correct (catalog). GOTCHA SOLVED: the agent-zinc box is **net-fenced to 127.99/16** → plain `127.0.0.1` TCP connect times out; the server binds `0.0.0.0` so reach it via a `127.99.x` address (gate uses `127.99.0.1`).
**Exact next step (pick one — perf levers now lead, per the head-to-head verdict):**
- **(A, DONE this cycle `2cb95904`) ~~PORT the B==1 matvec fast path to qwen `decodeBatch`~~** → done (qwen35-9b B1_TIMING 4.52→59.98 tok/s ~13.3×, BATCH_GATE+SANITY PASS, catalog 5/5). The whole catalog (gemma+qwen) now has the B==1 fast path. Remaining perf levers below.
- **(B, high-B win) the REAL serving-throughput lever — graph-capture the batched decode step** (Effort-25/27 playbook). The head-to-head B=8 gap is the batched-GEMM compute-inefficiency at B≥2 (where the B==1 matvec path does NOT apply) + per-launch overhead. Capture the `decodeBatch` step per distinct batch shape (shape changes on admit/evict → a small graph cache keyed by B). Util-gate the A/B. This is the lever that closes the aggregate gap vs `llama-server --parallel`.
- **(C, robustness) root-cause the sustained-throughput DEGRADATION** (round-2 collapse B=1 1.72→0.96, monotonic, not contention). Suspects to check FIRST: the per-call `pos_buf`/`slots_buf` `createBuffer`/`freeBuffer` in `decodeBatch` (2 cudaMalloc+2 cudaFree EVERY step — synchronizing + fragmenting; hoist to persistent scratch) and the per-row tail `commitAndWait`+download loop (B serial round-trips per step). Scheduler audited clean (release deinits, slots reuse, no unbounded growth).
- **(D, model generality, deprioritized) WIRE QWEN INTO THE SERVER.** The qwen `decodeBatch` is correctness-complete for the WHOLE catalog (dense qwen35 + MoE qwen36; 4c+4d done). But `runCudaServe` (`main.zig`) + `src/server/cuda_serve.zig::ServeEngine` are still **gemma-dense-only** — they construct a `ForwardGemma` and call `ForwardGemma.decodeBatch`/`allocSlotKv`. (Per the head-to-head this just serves MORE models all 42× behind → do a perf lever first.) To serve qwen35/36 + MoE:
- Generalize `ServeEngine` (and `runCudaServe`'s model-load + worker loop) to hold EITHER a `ForwardGemma` OR a `ForwardCuda`, dispatched by architecture at load time (a tagged union or a thin vtable over `allocSlotState`/`decodeBatch`/per-token-prefill). The `Scheduler` + `ReqChannel` + worker threading are model-agnostic — only the GPU-forward calls differ.
- qwen's per-token prefill into a slot: the harness uses `decodeBatch` B=1 per prompt token into the slot (the same path the gemma server uses). Reuse that. `allocSlotState(n_slots, slot_ctx)` already exists on `ForwardCuda` (4a).
- Re-run `scripts/validate_cuda_serve.sh` + the throughput gate with a qwen model (qwen35-9b dense first, then qwen36-35b-a3b MoE) → concurrent streams token-identical to isolated + catalog 5/5.
- (SEPARATE perf lever, deprioritized) 4d's `moeFfnBlockBatchedDecode` still LOOPS rows per-token — the throughput sub-step is to batch the router GEMM `[B,n_experts]` + `build_expert_order` over the B·n_used (token,expert) pairs + per-tensor `dmmv_*_experts` over B tokens (the MoE analog of 4c). Plus the still-open B==1 matvec fallback / graph-capture levers.

**Perf lever (SEPARATE, still open, deprioritized behind inc 4):** 3c showed the batched-decode path's B=1 ≈2.2 tok/s vs `decodeStep` ≈8 (gemma-31b) — crossover ~B≈3-4. Levers: (a) B==1 matvec fallback in `decodeBatch`; (b) CUDA-graph-capture the batched step per distinct B; (c) batched-prefill-on-admit. Util-gate any A/B; do NOT async gemma decode.
**Open risks / notes:** (1) `decodeBatch` is ARGMAX-identical (not bit-identical) to `decodeStep` — gate is token-identical, PASSED. (2) 3b locking: handlers lock only around `submit`/`nextChunk`/`finish`; the worker locks only to `publish` a token + flip `done`; ALL GPU + slot work is worker-only lock-free. `nextChunk` copies tokens out UNDER the lock (the worker's `tokens.append` may realloc) — never read `chan.tokens` unlocked. (3) prompt token buffer + `ReqChannel` are handler-owned: the handler drains to `finished` (worker done touching them) BEFORE freeing — `defer finish` then `defer free` (LIFO). (4) prefill still per-token B=1 into one slot (no cross-admit batching). (5) gemma-31b reloads ~17GB/call → server startup ~2 min; bump readiness timeouts well past 120s (gate uses 360s). (6) do NOT async gemma decode (proven regression). (7) throughput NOT measured until 3c (gemma decode boost-saturated → A/B before then is noise). (8) **default `dbg_cuda` model is QWEN** — pass the gemma path to `serve`/`sched`/`batch`. (9) gate-script footgun (FIXED): a bare `wait` also waits on the long-lived server bg job → hangs; `wait` only on the curl PIDs. (10) nohup buffers ~4KB → a backgrounded box script's tail can look stalled mid-run; check `ps wchan` + output files, not just the log tail. (11) serving is gemma4-dense-only (qwen/MoE serving = inc 4; `runCuda` errors clearly for qwen server mode). (12) net-fence: use `127.99.x` not `127.0.0.1` to reach the local server. (13) **NEW (3c): the batched-decode path is per-step SLOW vs the optimized `decodeStep`** — B=1 through `decodeBatch` ≈ 2.2 tok/s vs production single-stream gemma-31b ≈ 8 tok/s. Batching wins on AGGREGATE (B=8 → 15.9 tok/s = 7.34× the B=1 batched path) but NOT yet per-stream; crossover vs production single-stream is ~B≈3-4. `decodeBatch` has no CUDA-graph capture and at B=1 pays GEMM-vs-matvec overhead. This is the next perf lever (see Exact next step #2), NOT a correctness issue — TP_GATE (scaling + occupancy) PASSED and catalog stays 5/5. (14) 3c gate is server-side-counter-based (`/stats` diff) so it's robust to curl-start jitter; it does NOT yet compare vs `llama-server --parallel B` (left for a later head-to-head once the per-step path is competitive).

## Increments (ORDERED and DEPENDENT — do them in order; each is several cycles)

### Increment 1 — Batched-decode ISOLATED PROOF (gemma-31b dense, slot-based KV). THE ARCHITECTURAL CRUX.
Build a batched decode path for the **dense gemma-31b** (no SSM, no MoE — the cleanest forward), and PROVE it token-identical to N separate single-sequence runs. Sub-steps (commit each as it builds, all to `feat/e28-batching`; keep the production `decodeStep`/`prefillBatched` UNTOUCHED — batching is ADDITIVE behind a new entrypoint):
- **1a. Harness + slot KV.** Add a `dbg_cuda batch` subcommand (mirror the existing `dbg_cuda gen`/`gemm` harness pattern) taking B comma-sep token-id prompts; allocate slot-based KV `kv_k_slots[L] = [n_slots·max_ctx·kv_dim]` (small n_slots e.g. 4, modest max_ctx e.g. 2048 to keep VRAM/reloads cheap). No forward yet — just plumbing + a coherent single-slot smoke (B=1 must equal `gen`).
- **1b. Batched buffers + driver.** A `decodeBatch(tokens[B], positions[B], slots[B])` that reuses `BatchScratch` (B rows) for the GEMMs and runs the per-layer loop. Embed B rows, run norms/qkv/rope/o/ffn over B rows (all already B-capable), final norm + logits [B,vocab] + per-row argmax → B tokens.
- **1c. Per-sequence kv_write + the generalized attention kernel.** kv_write into each sequence's slot at its `positions[b]`. Add `gemma_attention_batched_seq` (or extend with a `positions[]`+slot-base arg): `seq_len = positions[b]+1`, K/V base offset by slot. Per-sequence causal + sliding window.
- **1d. THE PROOF.** Run B distinct prompts through `decodeBatch` for N steps (each advancing its own position), AND each prompt separately through `decodeStep`. **GATE: the B batched token streams must be TOKEN-IDENTICAL to the B serial streams.** Mixed positions (start sequences at different lengths) to exercise per-sequence `positions[]`. This proves no cross-contamination + correct per-seq KV/attention. Until 1d passes, increment 1 is NOT done.

### Increment 2 — Continuous-batching scheduler (host-side, ragged batch).
Wire `src/scheduler/scheduler.zig`: a running batch where sequences sit at **different positions** and **join/leave between steps**. Admit a new request (prefill it into a free slot via the existing single-sequence/prefillBatched path, then add it to the decode batch); evict on EOS or max_tokens and free its slot; admit a waiting request into the freed slot. Implement `pendingPrefill()`/`activeDecoding()` for real. **GATE: an interleaved arrival/exit schedule produces per-sequence outputs token-identical to isolated runs.**

### Increment 3 — Wire the server (the actual multi-tenant throughput win).
One GPU **worker thread** runs the continuous-batch loop; HTTP request threads enqueue into the scheduler and stream their tokens back (SSE) as their sequence produces them; relax/replace the global `generation_mutex`. **GATE: N concurrent HTTP clients get correct, isolated streams AND aggregate tok/s > the serialized baseline** (measure 1,2,4,8 concurrent; this is the headline number — compare to `llama-server --parallel`).

### Increment 4 — Extend to qwen (per-sequence SSM state) + batched MoE.
Per-sequence SSM recurrent state (a batch dim on the ssm-state buffers + the `ssm_delta_net` scan in `forward_cuda.zig`); batched MoE routing (reuse `build_expert_order` over the B tokens). **GATE: qwen35/36 + MoE catalog rows token-identical batched-vs-serial.**

### Increment 5 — Paged KV (memory efficiency → higher concurrency).
Replace slot-based fixed reservation with the existing `KvPagePool` block tables, so max concurrency isn't capped by `n_slots·max_ctx` VRAM. **GATE: correctness preserved + higher max concurrency at fixed VRAM.**

## Validation contract

- **The batched path is ADDITIVE.** The production single-sequence `decodeStep`/`prefillBatched`/server path must stay the default and stay correct at every commit. Add batched paths behind a new entrypoint / flag until increment 3 flips serving over — and even then, gate it.
- **Correctness gate (increments 1,2,4): batched == serial, TOKEN-IDENTICAL** per sequence (the `dbg_cuda batch` proof). Mixed positions required.
- **`scripts/validate_catalog.sh` must stay 5/5 token-correct** (`ZINC_GPU` = 5090 UUID) on the **serial** path every cycle — batching must not regress single-sequence inference.
- **Throughput gate (increment 3):** aggregate tok/s at B concurrent clients > serialized baseline, interleaved/util-gated, on the SAME 5090. Beat `llama-server --parallel B` is the bar.
- Isolated-cache builds (`ZIG_LOCAL_CACHE_DIR`+`ZIG_GLOBAL_CACHE_DIR`; verify the binary md5 changed or you are measuring stale code).

## HARD RULES

- Work in **this worktree** `/Users/stepan/Workspace/zinc-e28` on the **persistent branch `feat/e28-batching`** (accumulate increments here; push it each cycle). **Never** work in `/Users/stepan/Workspace/zinc` (main checkout) or `…/zinc-e26`/`…/zinc-e27`. **Never push to main.**
- **5090-pinned** (UUID `GPU-5126d018-ec86-be8b-1bf5-b5ac323d3350`): `export CUDA_VISIBLE_DEVICES=…` + `ZINC_GPU=…`. The box is **dedicated to this effort** now (the e26/e27 perf loops were stopped 2026-06-14). The 4090 (`GPU-e59a6fce-…`) is also free — use it for a parallel build/validate if helpful.
- Isolated box dir `~/zinc-e28`, **never** `~/workspace/zinc`. Box gotchas: `DECODE/PREFILL/GEN_IDS` print on **STDERR** (`2>&1`); `nohup … >FILE 2>&1 &` + poll the FILE (a backgrounded `ssh '… bash'` orphans the remote script); `pkill -f <pat>` self-matches the ssh argv (kill by PID); **gemma-31B reloads 18 GB/call (~45 s) and can WEDGE the WSL2 box's sshd** — keep B/max_ctx modest, batch your box work, and if SSH hangs past connect the box needs a human restart.
- **DO NOT async gemma decode** (boost-saturated, proven regression — Efforts 23/25). Orthogonal to batching; don't introduce it.
- ONE validated increment per cycle. Commit ONLY the scoped change. Update `## CURRENT STATE` (done-so-far + exact next step + risks) and append a Cycle log entry. Never commit host/IP/port. A blocked/negative cycle is still a logged finding — leave the tree clean.

## Cycle log

(append dated entries: cycle | increment+sub-step | change | built+md5-changed? | catalog 5/5 (serial)? | batched==serial proof result | branch/sha | exact next step)

- **2026-06-14 | inc 1 / sub-step 1a | `dbg_cuda batch` serial-reference harness + additive slot-based KV (`allocSlotKv`/`freeSlotKv`/`slotKvOffsetBytes`/`slotKvSmoke`) on `ForwardGemma`; production path untouched | built clean on the 5090, fresh isolated-cache (rm -rf .zig-cache + ZIG_GLOBAL_CACHE_DIR=/tmp/e28gc), bin md5 `50fcc2043743369e5077bbffba4716b4` | catalog 5/5 serial token-correct (12/12 each; qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | `batch` `BATCH_SEQ0`==`gen` seq0 (369…) AND `BATCH_SEQ1`==`gen` seq1 (240017…) token-identical; `SLOTKV_SMOKE:ok` | `feat/e28-batching` | NEXT: 1b — `decodeBatch(tokens[B],positions[B],slots[B])` for dense gemma-31b reusing BatchScratch + the batched GEMMs; wire allocSlotKv in for real; B=1 sanity ==gen before 1c generalizes per-seq positions/attention.**
- **2026-06-14 | inc 1 / sub-step 1c (PERF/structural; inc 1 COMPLETE) | fused the per-row decode attention loop into ONE batched launch each via two new kernels — `rms_norm_rope_qkv_seq` (grid.y=b batched V/Q/K norm+RoPE+slot-KV-write at per-seq positions[]/slots[] device arrays) + `gemma_attention_batched_seq` (grid.y=b batched per-seq causal/SWA softmax over each row's own slot KV, seq_len=positions[b]+1). `attentionLayerBatchedDecode` rewritten to 4 batched launches submitted async on the shared stream (no per-row commitAndWait); `decodeBatch` uploads positions[]/slots[] u32 device buffers once + single tail waitPending. Block math copied verbatim → bit-identical to 1b looped form. Production paths UNTOUCHED. | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), bin md5 `56cba1d62d2f73cdc9ef211712f83956` (≠1b 27e59fb8) | catalog 5/5 serial token-correct (12/12 each) | **B=5 mixed prompt-lengths (4/3/4/5/5), varying stream on seq3 (`954,236758,236813,107,236820,236758`): BATCH_GATE PASS + BATCH_SANITY PASS — token-identical fused-batched==solo==prod-gen across all 5 seqs** | `feat/e28-batching` `708e798a` | NEXT: Increment 2 — wire `src/scheduler/scheduler.zig` continuous-batching (admit/prefill into free slot → add to decode batch; evict on EOS/max → free slot; implement pendingPrefill/activeDecoding); GATE = interleaved arrival/exit schedule token-identical to isolated runs (extend the dbg harness, keep additive — don't touch the server mutex until Inc 3).**
- **2026-06-14 | inc 2 / sub-step 2a | wired `src/scheduler/scheduler.zig` as a real continuous-batching scheduler (additive): `pending` FIFO + `scratch`; new `enqueue`/`admitNext`/`hasFreeSlot`/`isIdle`; implemented `pendingPrefill()`/`activeDecoding()` for real (slot-IDs by state). New `dbg_cuda sched <seqs> <ngen> <nslots> [model]` harness (`schedMode`) drives a ragged running batch — staggered arrivals (seq j @ tick j·2), admit→per-token B=1 prefill into slot→`.decoding`, ONE `decodeBatch` over all decoders/tick (mixed positions), evict @ `ng` budget→free slot→admit waiter; nslots<nseq forces reuse. GATE `SCHED_GATE` = every scheduled stream token-identical to its isolated production run. Added a scheduler unit test. Production decode/server paths UNTOUCHED. | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), bin md5 `318411d9a153985d859360b0db6f99af` (≠1c `56cba1d6`) | catalog 5/5 serial token-correct (12/12 each) | **4 seqs mixed lengths (4/3/5/3), staggered arrivals, varying streams seq2 `1023,1024,53100,…`/seq3 `236774×3,15462×5`: `nslots=2` (slots 0+1 both reused) SCHED_GATE PASS completed=4/4; `nslots=1` (all 4 serialize through ONE slot, max reuse) SCHED_GATE PASS completed=4/4** — token-identical to isolated runs | `feat/e28-batching` (pending push) | NEXT: 2b — EOS-driven eviction w/ variable per-request gen lengths (re-run token gate); then Increment 3 — GPU worker thread runs this admit/decode loop, HTTP threads enqueue + SSE-stream, relax `generation_mutex`; GATE = N concurrent clients correct+isolated AND aggregate tok/s > serialized (beat `llama-server --parallel`).**
- **2026-06-14 | inc 2 / sub-step 2b | EOS-driven eviction + VARIABLE per-request gen lengths in `schedMode` (additive, harness+host-test only): pick an EOS token id (env `ZINC_SCHED_EOS`, else auto = seq0's mid-stream token; `maxInt` = pre-2b budget-only) applied to BOTH the isolated reference and the scheduled run via `req.shouldStop(eos)`; truncate references at first EOS (`serial_len[]`), record scheduled length in `finishSched` (`sched_len[]`), gate now requires token-identical streams INCLUDING matching EOS-truncated length → sequences leave the running batch at DIFFERENT ticks (mid-decode eviction → free slot → admit waiter while siblings keep decoding). New host-only scheduler unit test (EOS eviction frees a slot for a waiter). Production decodeStep/decodeBatch/server UNTOUCHED. | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), bin md5 `59f0db87a85b50796cef3fc2c39f89b0` (≠2a `318411d9`) | catalog 5/5 serial token-correct (12/12 each) | **`ZINC_SCHED_EOS=108`, 4 chat-template-shaped prompts: SEQ0 stops MID-DECODE at len=6 (`7611,1492,573,2134,235336,108`) while SEQ1/2/3 run full 12 → `nslots=2` SCHED_GATE PASS completed=4/4 (seq0 evicts mid-flight, slot reused by waiter); `nslots=3` SCHED_GATE PASS completed=4/4 — all token-identical incl. length.** auto-eos (constant synthetic streams→first-token EOS): lengths (1,1,12,12), `nslots=1`+`nslots=2` SCHED_GATE PASS. host scheduler/request unit tests 13/13 | `feat/e28-batching` `537306f2` | NEXT: Increment 3 — GPU worker thread runs the admit→prefill→decodeBatch→evict loop (reuse Scheduler API + decodeBatch); HTTP threads enqueue + SSE-stream their own tokens; relax/replace `generation_mutex` (routes.zig:351/554/605). GATE = N concurrent clients correct+isolated AND aggregate tok/s > serialized (beat `llama-server --parallel B`). First cycle to touch the server — keep serial catalog green + a single-seq fallback.**
- **2026-06-14 | inc 3 / sub-step 3a (first cycle toward wiring the server) | CONCURRENT SERVING-ENGINE PROOF. Finding: on CUDA there is NO HTTP server yet (`main.zig:1662` — prompt-mode only; `routes.zig`+`forward.zig::InferenceEngine` are Vulkan/Metal and don't use ForwardGemma) → Inc 3 = stand up a CUDA serving path, decomposed 3a(engine core)/3b(HTTP-SSE)/3c(throughput gate). 3a builds the server THREADING MODEL in isolation: new `dbg_cuda serve` harness (`serveMode`+`serveWorker`/`serveClient`/`serveFinish`+`ServeCtx`) — ONE GPU worker thread runs the admit→prefill→decodeBatch→evict loop (reuses Scheduler API + decodeBatch), N concurrent producer threads enqueue + receive their own stream via mutex+condvar. GPU work confined to the worker thread (CUDA shim rebinds ctx per call); only `pending` FIFO + result registry cross-thread, under one mutex; slot work lock-free. New host test 'concurrent enqueue under external mutex assigns unique slots'. Production decode/harness paths UNTOUCHED. | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), bin md5 `bfc7399fc2f589b89ddb287dabf0feff` (≠2b `59f0db87`) | catalog 5/5 serial token-correct (12/12 each) | **`serve` B=4 mixed-length prompts, nondeterministic concurrent enqueue order (req ids 1,3,2,4) → `nslots=2` (slot reuse) SERVE_GATE PASS 4/4; `nslots=4` (full batch) PASS 4/4; `nslots=2` budget-only PASS 4/4 — every client stream token-identical to its isolated single-seq reference incl. length.** host scheduler/request tests 14/14 | `feat/e28-batching` `d7a188d7` | NEXT: 3b — HTTP/SSE transport + a CUDA serve entrypoint (bring up a minimal CUDA HTTP path OR factor the 3a worker into a reusable serve module; per-connection enqueue + SSE-stream that request's own tokens; reuse src/server/http.zig). Keep CLI prompt path + serial catalog green. Then 3c — aggregate-tok/s gate vs `llama-server --parallel B`.**
- **2026-06-14 | inc 1 / sub-step 1b (+1d correctness) | `ForwardGemma.decodeBatch(tokens[B],positions[B],slots[B],out[B])` + `attentionLayerBatchedDecode`: batched DECODE for dense gemma — projections+FFN BATCHED over B rows (reuse `gemmDispatch`/`ffnBlockBatched`), attention inner LOOPED per row via single-token `rms_norm_rope_qkv`/`gemma_attention` on each seq's slot KV at its `positions[b]` (`seq_len=pos[b]+1`, slot-base offset). Harness extended: PASS A solo (B=1) + PASS B batched (B=nseq mixed positions) → `BATCH_GATE`(batched==solo) + `BATCH_SANITY`(solo==prod gen). Production decodeStep/prefillBatched UNTOUCHED. | built clean 5090, fresh isolated-cache, bin md5 `27e59fb85657f6c4981243bf7a467575` (≠1a) | catalog 5/5 serial token-correct (12/12 each) | **B=5 mixed prompt-lengths (4/3/4/5/5), varying stream on seq0 (`954,236758,236813,107,…`): BATCH_GATE PASS + BATCH_SANITY PASS — token-identical batched==solo==prod-gen across all 5 seqs** (also B=2 PASS) | `feat/e28-batching` `51b4af11` | NEXT: 1c is now PERF-ONLY — fuse the per-row attention loop into one `gemma_attention_batched_seq` (positions[]+slot-base, blockIdx.y=b) + batched per-seq kv-write, drop per-row syncs/async-submit; re-run the same proof (must stay PASS) + catalog. OR jump to Increment 2 (scheduler) — correctness crux already proven.**
- **2026-06-14 | inc 3 / sub-step 3b (first CUDA HTTP server) | stood up CUDA multi-tenant HTTP/SSE serving. New reusable `src/server/cuda_serve.zig::ServeEngine` (ONE GPU worker thread: admit→per-token-prefill→batched `decodeBatch`→evict, reusing Scheduler + decodeBatch from inc 1+2) with a per-request `ReqChannel` registry that streams each request's tokens INCREMENTALLY (the new piece vs 3a's whole-stream-at-finish): worker `publish`es each token under one mutex + broadcast; handler `nextChunk` blocks + copies out UNDER the lock (worker may realloc). `main.zig::runCudaServe` (reached when `zinc -m gemma` has NO --prompt): per-connection handler threads tokenize→submit→SSE-stream own tokens; routes /v1/completions, /v1/chat/completions, /health; `ZINC_SERVE_DEBUG_IDS=1` raw-token-id contract for exact gating, `ZINC_SCHED_EOS` overrides stop token. Additive — production decodeStep/prefillBatched + serve/sched/batch harnesses UNTOUCHED. New gate `scripts/validate_cuda_serve.sh`. | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), `zinc` bin md5 `6e625344a3056dda5e3b27f0b14dc93f` | catalog 5/5 serial token-correct (12/12 each) | **HTTP serve gate, B=4 mixed-length raw-token-id prompts, nslots=2 (slot reuse), NG=10, budget-only EOS: SERVE_HTTP_GATE_A PASS (concurrent N=4 == sequential B=1 through the server) + SERVE_HTTP_GATE_B PASS (server B=1 == dbg_cuda serve reference == isolated decodeStep) — every client SSE stream token-identical across all 4 seqs; dbg SERVE_GATE PASS published=4/4** | `feat/e28-batching` `6a290c0f` | NEXT: 3c — aggregate-tok/s gate, B=1/2/4/8 concurrent clients vs serialized + vs `llama-server --parallel B` on the same 5090 (decode launch/bw-bound → expect ~linear scaling); util-gate the A/B. Box gotcha SOLVED: net-fenced to 127.99/16 → reach the local server via 127.99.x, NOT 127.0.0.1; gate-script `wait` must target curl PIDs only (a bare `wait` hangs on the server bg job).**
- **2026-06-14 | inc 4 / sub-step 4a (qwen batched-decode plumbing; mirrors 1a) | added ADDITIVE per-sequence slot state to `ForwardCuda` (qwen35/36 hybrid-SSM): `kv_k_slots`/`kv_v_slots` (attn layers `[n_slots·slot_ctx·kv_dim]`) + `ssm_conv_slots`/`ssm_state_slots` (ssm layers `[n_slots·conv_state_len]`/`[n_slots·ssm_state_len]`, zero-init) + `n_slots`/`slot_ctx`; `allocSlotState`/`freeSlotState` (re-callable, stub for the wrong layer type → uniform free), `slotKvOffsetBytes(slot,pos)`/`slotConvOffsetBytes(slot)`/`slotStateOffsetBytes(slot)`, `slotStateSmoke` (non-overlap round-trip for KV + conv ring + recurrent state across slots). NEW vs gemma's slot KV = the **per-slot SSM conv + recurrent state** (the hybrid-SSM piece). `dbg_cuda batch` qwen branch (was a bare skip) now allocs slot state + runs the smoke; the qwen serial `BATCH_SEQ` reference (the 4b target) is emitted + cross-checks ==`gen`. Production `decodeStep`/`prefillStep`/single-seq buffers UNTOUCHED. | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), cuda-dbg md5 `86da205e7b19df9ec45502bc2e38d3af` | catalog 5/5 serial token-correct (12/12 each) | **`SLOTSTATE_SMOKE:ok` (slots=3 slot_ctx=512, KV+conv+recurrent non-overlap); qwen `BATCH_SEQ0` == `gen` `GEN_IDS` `11751,13,198,760,6511,314,9338,369` (serial reference == production decode)** | `feat/e28-batching` (commit pending push of this doc) | NEXT: 4b — `ForwardCuda.decodeBatch(tokens[B],positions[B],slots[B],out[B])`: batched projections/FFN over B rows (reuse prefill GEMM path) + per-row attn (kv-write/`naive_attention` into the slot KV at `pos[b]`) + per-row SSM (conv1d/delta_net/gated_norm reading/writing each slot's own conv+recurrent state; per-slot conv ring offset). Use DENSE qwen35-9b for the 4b proof (MoE→4d). Gate: extend `dbg_cuda batch` qwen branch with PASS-A-solo/PASS-B-batched → `BATCH_GATE`+`BATCH_SANITY` token-identical, mixed positions; catalog stays 5/5.**
- **2026-06-14 | inc 4 / sub-step 4b (qwen batched decodeBatch — the qwen analog of gemma's 1b) | new `ForwardCuda.decodeBatch(tokens[B],positions[B],slots[B],out[B])` for DENSE qwen35 (hybrid-SSM): a per-row loop runs each sequence's forward against its OWN slot state via two slot-variant helpers copied VERBATIM from production `attentionLayer`/`ssmLayer` (block math identical → batched==N-serial token-identical by construction). `attentionLayerSlot(L,pos,slot)` = KV aliased to the slot region (`kv_base=slot·slot_ctx·kv_dim·4`), kv-write + `naive_attention` pos-relative within the slot (`seq_len=pos+1`, sinks + sigmoid-z gate), synchronous `commitAndWait`. `ssmLayerSlot(L,pos,slot)` = conv ring + recurrent state aliased to the slot (the new hybrid-SSM piece), `conv_off` DERIVED `=pos%(d_conv-1)`. MoE (`n_experts>0`) → `error.Unsupported` (4d). Added `resetState()` (zero prod single-seq SSM conv+recurrent + clear conv_off) so each harness SERIAL reference is truly single-sequence (qwen's unindexed SSM state would leak; no-op for gemma). `dbg_cuda batch` qwen branch: 4a smoke → PASS-A-solo / PASS-B-batched proof (per-token B=1 prefill into each slot, then decode all seqs together at MIXED positions). Production decodeStep/prefillStep/single-seq buffers UNTOUCHED. | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), cuda-dbg md5 `6c3e7194823e65524ce298fc057c727b` (≠4a `86da205e`) | catalog 5/5 serial token-correct (12/12 each) | **qwen35-9b, 3 seqs mixed prompt-lengths (4/3/5), mixed positions, varied seq0 stream (`5,6,24218,10,4838,1665,15,16`): SLOTSTATE_SMOKE:ok (slots=3 slot_ctx=512); BATCHDEC_SEQ0/1/2 all gate=MATCH sanity=MATCH → BATCH_GATE:PASS + BATCH_SANITY:PASS** — token-identical batched==solo==serial across all 3 seqs | `feat/e28-batching` `3b2268fc` | NEXT: 4c — FUSE the per-row loops into batched grid.y=b kernels (batch the dense qkv/o/ffn GEMMs over B rows via the prefill GEMM path = the amortization win; qwen-analog of `gemma_attention_batched_seq` for the per-seq attention inner incl. packed-gate deinterleave + per-head q/k norm; batched per-seq SSM conv/delta_net/gated_norm with per-row `conv_off`). Re-run the qwen proof (must stay PASS) + catalog 5/5. Then 4d batched MoE (qwen36-35b-a3b) lifts the Unsupported guard.**
- **2026-06-14 | inc 4 / sub-step 4c-step-1 (qwen batched dense GEMMs — the amortization win; qwen analog of gemma's 1b structure) | qwen `forward_cuda.zig` had NO batched-GEMM path on this branch (e26's qwen prefillBatched is on main, post fork-point) → BUILT it: new `gemm:[4]CudaPipeline`+`gemm_f32` (the generic `gemm_*_tiled_v2` kernels already in kernels.cu + used by gemma; token-major `[T,M]` out, `acc_mode` honored for the residual — verified `kernels.cu:1719`), `GemmPush` (byte-matches kernels.cu), `gemmDispatch(w,x,y,M,K,B,acc_mode)`, and a lazily-allocated token-major `[B,dim]` `DecodeBatch` scratch (dedicated `ssm_delta`/`ssm_y` `[B,d_inner]` buffers so SSM intermediates never alias with conflicting strides). `decodeBatch` REWRITTEN layer-major: embed all B rows into `db.hidden` slices (same embed kernel as serial → bit-aligned), then per layer the attn qkv/o + ssm qkv/z/alpha/beta/out + ffn gate/up/down run as ONE batched `gemm` each over B rows (each weight read ONCE); the per-seq inner (deinterleave/q-k-norm/RoPE/KV-write/softmax/gate; conv1d/delta_net/gated_norm) still LOOPS per row on row b's slice + its slot KV/conv/recurrent state (block math copied verbatim). FFN FULLY batched. Per-row argmax tail. 4b's `attentionLayerSlot`/`ssmLayerSlot` now superseded/dead (kept as reference). Production `decodeStep`/`prefillStep`/single-seq buffers UNTOUCHED — serial catalog never runs the new `gemm` pipelines. | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), cuda-dbg md5 `c84ed3f966ee5c15e6cc0299a7256957` (≠4b `6c3e7194`) | catalog 5/5 serial token-correct (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **qwen35-9b, 3 seqs mixed prompt-lengths (4/3/5), mixed positions, varied seq0 stream (`4838,1665,15,16,17,18,19,20`): SLOTSTATE_SMOKE:ok (slots=3 slot_ctx=512); BATCHDEC_SEQ0/1/2 all gate=MATCH sanity=MATCH → BATCH_GATE:PASS + BATCH_SANITY:PASS** — token-identical batched==solo==serial across all 3 seqs (ARGMAX-identical: GEMM-vs-matvec reduction order, not bit-identical; token-gate PASSED) | `feat/e28-batching` (commit pending) | NEXT: 4c-step-2 — fuse the per-seq attention + SSM inner loops into batched grid.y=b kernels (drop the per-row `commitAndWait`s; the `db.*` buffers are already token-major `[B,dim]`); re-run the proof (stay PASS) + catalog. Then 4d batched MoE, then wire qwen into the server.**
- **2026-06-14 | inc 4 / sub-step 4c-step-2a (qwen batched-decode attention-inner FUSION; qwen analog of gemma's 1c) | fused the per-row decode-attention loop into TWO batched grid.y=B kernels (added to kernels.cu): `qwen_norm_rope_qkv_seq` (grid=(n_head+2·n_kv_head, B): per-(head,b) deinterleave from interleaved qfull + per-head q/k RMS-norm + RoPE + slot KV write at per-seq positions[]/slots[] + raw-gate extract; V copied raw — qwen does NOT normalize V; per-(head,b) math copied verbatim from deinterleave_qgate + rms_norm(N=head_dim) + rope + kv_cache_write) + `naive_attention_batched_seq` (grid=(n_head, B): per-(head,b) 3-pass softmax(QK^T)V + attention-sink rescale over each row's slot KV, seq_len=positions[b]+1; copied verbatim from naive_attention). `attentionLayerBatchedDecode` REWRITTEN: ONE command buffer / ONE commitAndWait (was B per-row commitAndWait) = batched pre-norm+Q(+gate)/K/V GEMMs → fused qkv → batched attn → sigmoid_mul over [B,q_dim] (existing kernel, token-major contiguous) → batched O proj. `decodeBatch` uploads positions[]/slots[] device u32 buffers once + max_seq_len (attn smem). SSM inner unchanged (4c-step-2b). Production decodeStep/prefillStep/single-seq UNTOUCHED (serial path never runs the new `_seq` kernels). | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), cuda-dbg md5 `96f70e14e5d25546c2196f4d9a5ba5fe` (≠4c-1 `c84ed3f9`) | catalog 5/5 serial token-correct (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **qwen35-9b, 3 seqs mixed prompt-lengths (4/3/5), mixed positions: SLOTSTATE_SMOKE:ok (slots=3 slot_ctx=512); BATCHDEC_SEQ0 `369,11751,13,198,760,6511,314,9338`/SEQ1/SEQ2 all gate=MATCH sanity=MATCH → BATCH_GATE:PASS + BATCH_SANITY:PASS** — token-identical batched==solo==serial (ARGMAX-identical; GEMM reduction order, token-gate PASSED) | `feat/e28-batching` `6a569531` | NEXT: 4c-step-2b — fuse the per-seq SSM scan inner (ssm_conv1d/delta_net/gated_norm) into batched grid.y=b kernels reading each slot's conv ring + recurrent state at per-row `conv_off=pos%(d_conv-1)`; collapse `ssmLayerBatchedDecode` to one commitAndWait; re-run proof (stay PASS) + catalog. Then 4d batched MoE, then wire qwen into the server.**
- **2026-06-14 | inc 4 / sub-step 4c-step-2b (qwen batched-decode SSM-inner FUSION → 4c COMPLETE) | fused the per-row decode SSM loop (conv1d / delta-net scan / gated-norm) in `ssmLayerBatchedDecode` into THREE batched grid.y/z=B kernels (added to kernels.cu) — the qwen analog of 4c-2a's attention fusion + the genuinely-new hybrid-SSM batched work (gemma had no SSM): `ssm_conv1d_seq` (grid=(ceilDiv(conv_channels,64), B): per-(channel,b) depthwise causal conv + SiLU over each row's slot conv ring `ssm_conv_slots[L]` at per-row state_offset = positions[b] % (d_conv-1); input/output token-major [B,conv_channels]; per-channel math copied verbatim from ssm_conv1d, state_offset derived from positions[b]) + `ssm_delta_net_seq` (grid=(dt_rank, head_v_dim, B): per-(head,row,b) gated delta-net scan over each row's slot recurrent state `ssm_state_slots[L]` at slot base slots[b]·ssm_state_len, ONE decode token per row — no n_tok loop; per-(h,row,b) math copied verbatim from ssm_delta_net, β/δ locals renamed to avoid the b=blockIdx.z clash) + `ssm_gated_norm_seq` (grid=(dt_rank, B): per-(head,b) gated RMS-norm over token-major rows, norm-weight indexed by head_base only NOT the row offset; copied verbatim from ssm_gated_norm). `ssmLayerBatchedDecode` REWRITTEN: ONE command buffer / ONE commitAndWait (was B per-row commitAndWait) = batched pre-norm + qkv/z/α/β GEMMs → ssm_conv1d_seq → ssm_delta_net_seq → ssm_gated_norm_seq → batched out-proj accumulate. `decodeBatch` passes the already-uploaded `pos_buf`/`slot_buf` device arrays (was host slices). The 4b/4c-1 per-row `ssmLayerSlot` reference is unchanged (still dead). Production decodeStep/prefillStep/single-seq UNTOUCHED (serial path never runs the new `_seq` kernels). | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), cuda-dbg md5 `ef484e4058f2df9e789a41f3dc362d6b` (≠4c-2a `96f70e14`) | catalog 5/5 serial token-correct (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **qwen35-9b, 3 seqs mixed prompt-lengths (4/3/5), mixed positions: SLOTSTATE_SMOKE:ok (slots=3 slot_ctx=512); BATCHDEC_SEQ0 `4838,1665,15,16,17,18,19,20`/SEQ1 `544,445,49457,477,198,16,15,13`/SEQ2 `6,24218,10,4838,1665,15,16,17` all gate=MATCH sanity=MATCH → BATCH_GATE:PASS + BATCH_SANITY:PASS** — token-identical batched==solo==serial (ARGMAX-identical; GEMM reduction order, token-gate PASSED) | `feat/e28-batching` `bd39d484` | NEXT: 4d — batched MoE over B rows (qwen36-35b-a3b): lift the `if (d.n_experts>0) return error.Unsupported` guard in `decodeBatch`; batch the MoE-FFN (router GEMM [B,n_experts] → top-k → `build_expert_order` over B·n_used pairs → per-tensor `dmmv_*_experts` expert matvec → weighted combine + shared expert per row); extend the `dbg_cuda batch` qwen path to qwen36-35b-a3b → stay BATCH_GATE/SANITY PASS + catalog 5/5. THEN wire qwen into `runCudaServe`/`ServeEngine` (currently gemma-dense-only).**
- **2026-06-14 | inc 4 / sub-step 4d (batched-decode MoE FFN over B rows → qwen `decodeBatch` correctness-complete for the whole catalog) | lifted the `if (d.n_experts>0) return error.Unsupported` guard in `ForwardCuda.decodeBatch`; the per-layer FFN now routes to a new `moeFfnBlockBatchedDecode` when `n_experts>0` (else `ffnBlockBatchedDecode`). KEY INSIGHT: the MoE FFN is STATELESS (no KV, no SSM recurrent state — reads `hidden`, routes to experts, accumulates back into `hidden`) so unlike attn/SSM it needs NO per-slot state and NO per-seq position → each of the B rows is an INDEPENDENT single-token MoE FFN. `moeFfnBlockBatchedDecode` LOOPS the rows, aliases row b's `db.hidden` slice, runs the production `moeFfnBlock` body VERBATIM on it via `moeFfnRowDecode` (rms_norm → router top-k → routed experts SwiGLU+down via the batched-q4k/q5k fast path OR the per-quant host-id fallback, byte-identical to production → weighted combine → shared expert), reading/accumulating into the row alias + a synchronous `waitPending` per row so the shared per-row scratch is safe to reuse → batched==N-serial token-identical by construction (mirrors the 4b→4c method). `dbg_cuda batch` qwen branch drops the MoE skip (was `BATCHDEC:skip MoE — 4d`). Production `moeFfnBlock`/`decodeStep`/`prefillStep` UNTOUCHED. NOTE: 4d is the CORRECTNESS step — the row loop does NOT yet read each expert weight once across B (the throughput sub-step = batch router GEMM [B,n_experts] + `build_expert_order` over B·n_used + per-tensor `dmmv_*_experts`, the MoE analog of 4c). | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), cuda-dbg md5 `53992af4f9f5b413d7b68a6006e5f163` (≠4c-2b `ef484e40`) | catalog 5/5 serial token-correct (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **qwen36-35b-a3b (256 experts, 8 active, SSM+MoE), 3 seqs mixed prompt-lengths (4/3/5), mixed positions: SLOTSTATE_SMOKE:ok (slots=3 slot_ctx=512); BATCHDEC_SEQ0 `4838,1665,15,16,17,18,19,20`/SEQ1 `198,410,268,410,198,2,148929,152402`/SEQ2 `15,16,17,18,19,20,21,22` all gate=MATCH sanity=MATCH → BATCH_GATE:PASS + BATCH_SANITY:PASS** — token-identical batched==solo==serial through the SSM + MoE path (ARGMAX-identical; GEMM reduction order, token-gate PASSED) | `feat/e28-batching` `12b1c04e` | NEXT: wire qwen into the server (`runCudaServe`/`ServeEngine` are gemma-dense-only → generalize to dispatch ForwardCuda(qwen)/ForwardGemma by arch; re-run validate_cuda_serve + throughput with qwen35-9b then qwen36-35b-a3b). Then the MoE throughput sub-step + B==1/graph perf levers.**
- **2026-06-14 | inc 3 / sub-step 3c (INCREMENT 3 COMPLETE) | the aggregate-throughput gate — the HEADLINE. Added `ServeEngine` throughput counters (`decode_steps`/`decode_tokens`/`decode_wall_ns`/`prefill_tokens`/`prefill_wall_ns`/`peak_batch`, worker-thread-only `fetchAdd` writes, `/stats` handler reads via atomic load — lock-free); `decode_tokens` SUMS the per-step batch occupancy so `Δdecode_tokens/Δdecode_wall_s` = aggregate decode tok/s and `Δdecode_tokens/Δdecode_steps` = mean occupancy. New `GET /stats` route (`main.zig`→`statsJson`). New `scripts/throughput_cuda_serve.sh`: builds + starts the server once, warms, runs B=1/2/4/8 concurrent-client phases × ROUNDS, diffs `/stats` around each phase + prints external sanity tok/s. Additive — production decodeStep/prefillBatched + serve/sched/batch harnesses UNTOUCHED. | built clean 5090, fresh isolated-cache (ZIG_GLOBAL_CACHE_DIR=/tmp/e28gc), `zinc` bin md5 `6c2ab9a41b5fa2d64acd936b88833fbe` (≠3b `6e625344`) | catalog 5/5 serial token-correct (12/12 each) | **gemma-31b dense, ctx=1024, NG=96, nslots=8, 3 rounds, budget-only EOS: srv decode tok/s 2.2/4.2/8.3/15.9 = 1.00/1.95/3.81/7.34× vs B=1; occupancy 1.00/1.98/3.96/7.92 (batch fills) → TP_GATE_SCALING PASS + TP_GATE_OCCUPANCY PASS + TP_GATE PASS** — near-linear aggregate decode scaling to B=8 over the real HTTP server | `feat/e28-batching` `8d29967d` | NEXT: Increment 4 (qwen per-seq SSM state + batched MoE → serve qwen/MoE not just gemma-dense) — OR the SEPARATE perf lever: the batched-decode path is per-step slow (B=1 ≈2.2 vs production decodeStep ≈8 tok/s; no CUDA-graph capture, B=1 GEMM-vs-matvec overhead) so crossover vs single-stream is ~B≈3-4 → (a) B==1 matvec fallback, (b) graph-capture per distinct batch shape, (c) batched-prefill-on-admit. Util-gate any A/B. Box gotchas held: gemma 17GB reload ~2min server start; net-fence 127.99.x.**
- **2026-06-14 | PERF LEVER — B==1 matvec fast path in gemma `decodeBatch` | pivoted off inc-4 server-wiring to the perf lever the 2026-06-15 head-to-head flagged (zinc serving ~30-42× behind llama-server; per-STREAM path ~4× slower than `decodeStep`). Root cause (code-read): every B==1 `decodeBatch` step (ALL per-token prefill + single-client decode) ran its per-layer projection/FFN through the 64×64-tiled batched TC GEMM, which processes ONE row + wastes 63/64 of the tile (cuBLAS self-gates off at T=1<128). FIX (additive, surgical): `decode_b1` flag on `ForwardGemma` set in `decodeBatch` ONLY when `B==1` AND `b1MatvecOn()` (default-on, opt-out `ZINC_BATCH_B1_MATVEC=0`; `defer`-reset so a stale true never leaks into `prefillBatched`) + a guard at the TOP of `gemmDispatchA` (`if T==1 and self.decode_b1 → dmmvDispatch`, the SAME tuned matvec `decodeStep` uses; drop-in since every decode GEMM is acc_mode 0 + contiguous at T==1). Fused per-seq attn kernels unchanged (work at grid.y=1). Harness-only `decode_b1_force: ?bool` override + a `B1_TIMING` block in the `dbg_cuda batch` gemma branch time both paths in ONE model load (boost-comparable A/B). Production `decodeStep`/`prefillBatched` UNTOUCHED (serial never sets `decode_b1`). | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), cuda-dbg md5 `bb3982e196dca4c7b2cc0e2ed3701ed4` (≠4d `53992af4`) | catalog 5/5 serial token-correct (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **gemma-31b, 3 seqs mixed positions (4/3/5): SLOTKV_SMOKE:ok; BATCHDEC_SEQ0/1/2 all gate=MATCH sanity=MATCH → BATCH_GATE:PASS + BATCH_SANITY:PASS (matvec path on by default); B1_TIMING in-process A/B (NG=64): matvec OFF 2.19 → ON 46.37 tok/s** (OFF matches the documented "B=1 ≈2.2" → the penalty is gone; the boost-comparable one-load RATIO is the clean claim, not the cross-context 46-vs-8) | `feat/e28-batching` `0fee12cc` | NEXT (perf-led): (A) port this B==1 fast path to qwen `decodeBatch` (`forward_cuda.zig`, same penalty); (B) graph-capture the batched decode step (the high-B aggregate lever — B≥2 GEMM inefficiency); (C) root-cause the sustained-throughput degradation (suspect: per-call `pos_buf`/`slots_buf` cudaMalloc/free in `decodeBatch` + per-row tail sync loop); (D, deprioritized) wire qwen into the server.**
- **2026-06-14 | PERF LEVER — B==1 matvec fast path PORTED to qwen `decodeBatch` (`forward_cuda.zig`) | the qwen analog of gemma's `0fee12cc`: qwen's 4c `gemmDispatch` over B rows runs at B==1 for every per-token prefill + single-client decode → 63/64 of the 64×64 tile wasted. FIX (additive, mirrors gemma): `decode_b1`+`decode_b1_force` flags on `ForwardCuda`; set in `decodeBatch` ONLY when `B==1` AND `b1MatvecOn()` (default-on, opt-out `ZINC_BATCH_B1_MATVEC=0` — SAME env knob as gemma; `defer`-reset); guard at the TOP of `gemmDispatch`: `if (B==1 and self.decode_b1) → dmmvDispatch` (the tuned matvec `decodeStep` uses). KEY DIFF vs gemma: passes `acc_mode` THROUGH (qwen O-proj/FFN-down/SSM-out use `acc_mode=1`, unlike gemma's all-0 decode GEMMs); `dmmvDispatch` honors acc_mode+a_offset(=0 contiguous at B==1) and handles every weight type incl. f32 (`dmmvIdx` f32→4=`dmmv_f32`; prod `decodeStep` SSM projections already route f32 through it). Fused `_seq` attn/SSM kernels work at B=1 unchanged. Harness `B1_TIMING` A/B added to the `dbg_cuda batch` qwen branch (mirror gemma). Production `decodeStep`/`prefillStep` UNTOUCHED (serial never sets `decode_b1`). | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), cuda-dbg md5 `07e46933f3f37a13dfeca2382f97fa29` (≠ gemma-fast-path `bb3982e1`) | catalog 5/5 serial token-correct (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **qwen35-9b, 3 seqs mixed prompt-lengths (4/3/5), mixed positions: SLOTSTATE_SMOKE:ok; BATCHDEC_SEQ0/1/2 all gate=MATCH sanity=MATCH → BATCH_GATE:PASS + BATCH_SANITY:PASS (matvec path on by default); B1_TIMING in-process A/B (NG=64): matvec OFF 4.52 → ON 59.98 tok/s (~13.3×)** | `feat/e28-batching` `2cb95904` | NEXT (perf-led): (B) graph-capture the batched decode step (the HIGH-B aggregate lever — B≥2 GEMM compute-inefficiency where the matvec path doesn't apply; Effort-25/27 playbook, graph cache keyed by batch shape); (C) root-cause the sustained-throughput degradation (suspect: per-call `pos_buf`/`slot_buf` cudaMalloc/free in `decodeBatch` + per-row tail sync loop — hoist to persistent scratch); (D, deprioritized) wire qwen into the server (`runCudaServe`/`ServeEngine` gemma-dense-only → dispatch ForwardCuda/ForwardGemma by arch). Both gemma+qwen `decodeBatch` now have the B==1 fast path → no low-B per-stream penalty for the whole catalog.**
